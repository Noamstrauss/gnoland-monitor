apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus:${PROMETHEUS_PORT}
    basicAuth: false
    isDefault: false
    version: 1
    editable: true
    uid: prometheus